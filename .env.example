# AI-Enhanced Crypto Exchange Configuration
# Copy to .env and fill in values

# Anthropic API key for Claude-powered anomaly detection (optional)
# Without this, the system falls back to rule-based detection
ANTHROPIC_API_KEY=

# Server config
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=info
